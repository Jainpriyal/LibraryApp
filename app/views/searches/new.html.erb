<h1>Advanced Search</h1>

<%= form_for @search, html: {class: "pure-form"} do |s| %>

<div class="field">
  <%= s.label :roomNumber %><br />
  <%= s.text_field :roomNumber %>
</div>

    <div class="field">
    <%= s.label :size %><br>
    <%= s.select :size, ['small', 'medium', 'large'] %>
    </div>
    <div class="field">
      <%= s.label :status %><br>
      <%= s.select :status, ['Available', 'Booked'] %>
    </div>
    <div class="field">
      <%= s.label :building %><br>
      <%= s.select :building, ['Hunt', 'Hill' ] %>
    </div>

    <div class = "action"><%= s.submit "Search", class: "pure-button pure-button-primary" %></div>

<% end %>

<p> <%= link_to 'Back', rooms_path %></p>
